# microservices-user-management
A Node.js microservices architecture for user management — featuring an API Gateway, Auth Service (JWT), User Service (CRUD), and Notification Service, all backed by MongoDB.
